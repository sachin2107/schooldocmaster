CREATE TABLE PROJ.REFERENCE_DATA_ELEMENT(
	ELEMENT_CODE varchar(30) NOT NULL,
	CATEGORY_CODE varchar(30) NOT NULL,
    ACTIVE_FLAG char(1) NOT NULL,
    ELEMENT_DESC varchar(1024) NOT NULL,
    ELEMENT_VALUE varchar(1024) NOT NULL,
    ORDER_SEQ int NOT NULL,
    ACTIVE_TIMESTAMP timestamp,
    EXPIRY_TIMESTAMP timestamp,
    UPDATE_USER_ID varchar(64) NOT NULL,
    UPDATE_TIMESTAMP timestamp,
    LOCALE varchar(30) NOT NULL,
    CHANNEL_ID varchar(30) NOT NULL,
    CREATED_USER_ID varchar(64) NOT NULL,
    CREATED_TIMESTAMP timestamp,
    CONSTRAINT REFERENCE_DATA_EL1 PRIMARY KEY (CATEGORY_CODE,LOCALE,CHANNEL_ID,ELEMENT_CODE)
);

CREATE INDEX MDM_RF2 ON PROJ.REFERENCE_DATA_ELEMENT
(
	CATEGORY_CODE,ELEMENT_CODE
);

CREATE INDEX MDM_RF1 ON PROJ.REFERENCE_DATA_ELEMENT
(
	ELEMENT_CODE,
    CATEGORY_CODE,
    ACTIVE_FLAG
);

CREATE UNIQUE INDEX REFERENCE_DATA_EL1 ON PROJ.REFERENCE_DATA_ELEMENT
(
	CATEGORY_CODE,
    LOCALE,
    CHANNEL_ID,
    ELEMENT_CODE
);

--create table proj.reference_data_element (LOCALE varchar(30) not null, ELEMENT_CODE varchar(30) not null, CHANNEL_ID varchar(30) not null, CATEGORY_CODE varchar(30) not null, ACTIVE_FLAG char(1), ACTIVE_TIMESTAMP varchar(30), CREATED_TIMESTAMP datetime, CREATED_USER_ID varchar(30), ELEMENT_DESC varchar(30), ELEMENT_VALUE varchar(30), EXPIRY_TIMESTAMP varchar(30), ORDER_SEQ integer, UPDATE_USER_ID varchar(30), UPDATE_TIMESTAMP datetime, primary key (LOCALE, ELEMENT_CODE, CHANNEL_ID, CATEGORY_CODE));